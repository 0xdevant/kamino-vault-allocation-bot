# dependencies
node_modules/

# build output
dist/

# secrets & local config — NEVER commit these
.env
.env.*
!.env.example
*.keypair.json
keypair*.json
id.json
wallet*.json

# runtime state (rolling history buffer used by delta-based risk signals)
history.json
data/history.json
*.local.json

# logs & os noise
*.log
.DS_Store

